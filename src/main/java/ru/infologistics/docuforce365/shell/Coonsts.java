package ru.infologistics.docuforce365.shell;

/** Utility constants used across the application. */
public final class Coonsts {
  public static final String HEADER_CORRELATION_ID = "Correlation-Id";
  public static final String CORRELATION_ID_LOG_VAR = "correlationId";
  public static final String CORRELATION_ID_PATTERN = "^[0-9A-Za-z]{8}$";

  private Coonsts() {
  }
}
